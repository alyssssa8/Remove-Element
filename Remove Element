class Solution {
public:
    int removeElement(vector<int>& nums, int val) {
        int traverse = 0;
        for(int i = 0; i < nums.size(); i++){
            if (nums[i] != val){
               nums[traverse] = nums[i];
               traverse++;
            }
        }
     return traverse;   
    }
};
